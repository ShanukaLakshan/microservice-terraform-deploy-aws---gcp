services:
  fastapi:
    build: .
    container_name: microservice-terraform-deploy-aws-gcp
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: always
